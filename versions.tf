# Azure Firezone Gateway Terraform Version Requirements
# Equivalent to GCP terraform-google-gateway versions

terraform {
  required_version = ">= 1.0"
  
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 3.0"
    }
  }
}